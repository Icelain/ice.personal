<!DOCTYPE HTML>
<html lang="en">
<head>
	<title>Blog — icelain</title>
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<style>


		@font-face {
	        font-family: "Agave";
	        src: url("/static/agave.woff2") format("woff2");
	    }

	    @font-face {
	        font-family: "Geist";
	        src: url("/static/geist.woff2") format("woff2");
	    }
				
		* {
			margin: 0;
			padding: 0;
			box-sizing: border-box;
		}
		
		html, body {
			background-color: #101010;
			color: #FFFFFF;
			font-family: 'Geist';
			font-size: 14px;
			line-height: 1.6;
			min-height: 100vh;
			font-weight: 400;
		}

		::selection {
			background: #232323;
			color: #FFCFA8;
		}

		::-moz-selection {
			background: #232323;
			color: #FFCFA8;
		}
		
		body {
			display: flex;
			flex-direction: column;
		}
		
		.nav {
			padding: 20px 24px;
			border-bottom: 1px solid #151515;
			display: flex;
			align-items: center;
			justify-content: space-between;
		}
		
		.nav-left {
			display: flex;
			align-items: center;
			gap: 12px;
		}
		
		.logo {
			font-size: 16px;
			font-weight: 500;
			text-decoration: none;
			color: #FFCFA8;
		}

		.separator {
			color: #505050;
			font-size: 14px;
		}

		.page-title {
			color: #A0A0A0;
			font-size: 14px;
			font-weight: 400;
		}
		
		.nav-link {
			color: #65737E;
			text-decoration: none;
			font-size: 13px;
		}
		
		.nav-link:hover {
			color: #FFCFA8;
		}
		
		.container {
			max-width: 900px;
			margin: 0 auto;
			padding: 40px 24px;
			flex: 1;
			width: 100%;
		}
		
		.header-section {
			margin-bottom: 48px;
		}

		h1 {
			font-size: 28px;
			font-weight: 500;
			color: #FFCFA8;
			margin-bottom: 8px;
		}

		.description {
			color: #A0A0A0;
			font-size: 14px;
		}
		
		.blog-list {
			border: 1px solid #151515;
		}
		
		.blog-item {
			padding: 20px;
			border-bottom: 1px solid #151515;
			position: relative;
			background: #101010;
			display: block;
			text-decoration: none;
			color: inherit;
			transition: all 0.2s ease;
		}
		
		.blog-item:last-child {
			border-bottom: none;
		}
		
		.blog-item:hover,
		.blog-item:focus {
			background: #FFCFA8;
			padding-left: 24px;
			outline: none;
		}

		.blog-item:focus {
			box-shadow: inset 0 0 0 2px #FFCFA8;
		}

		.blog-item:hover .title-text,
		.blog-item:focus .title-text {
			color: #101010;
		}

		.blog-item:hover .meta,
		.blog-item:focus .meta {
			color: #101010;
		}

		.blog-item::before {
			content: '';
			position: absolute;
			left: 0;
			top: 0;
			width: 0;
			height: 100%;
			background: #FFCFA8;
			transition: width 0.2s ease;
		}

		.blog-item:hover::before,
		.blog-item:focus::before {
			width: 3px;
		}
		
		.title {
			margin: 0 0 6px 0;
			font-size: 16px;
			font-weight: 500;
		}
		
		.title-text {
			color: #FFCFA8;
			text-decoration: none;
		}
		
		.meta {
			font-size: 12px;
			color: #65737E;
			display: flex;
			align-items: center;
			gap: 8px;
		}

		.date {
			display: flex;
			align-items: center;
			gap: 4px;
		}

		.date::before {
			content: '•';
			color: #505050;
		}
		
		.empty-state {
			text-align: center;
			padding: 60px 24px;
			color: #65737E;
		}

		footer {
			padding: 20px 24px;
			border-top: 1px solid #151515;
			text-align: center;
			color: #505050;
			font-size: 12px;
		}
		
		@media (max-width: 768px) {
			.nav {
				padding: 16px;
				flex-direction: column;
				align-items: flex-start;
				gap: 12px;
			}

			.nav-left {
				gap: 8px;
				flex-wrap: wrap;
			}

			.logo {
				font-size: 15px;
			}

			.page-title {
				font-size: 13px;
			}
			
			.container {
				padding: 24px 16px;
			}

			.header-section {
				margin-bottom: 32px;
			}

			h1 {
				font-size: 22px;
			}

			.description {
				font-size: 13px;
			}
			
			.blog-item {
				padding: 16px;
			}

			.blog-item:hover,
			.blog-item:focus {
				padding-left: 20px;
			}
			
			.title {
				font-size: 14px;
			}
			
			.meta {
				font-size: 11px;
			}
		}

		@media (max-width: 480px) {
			.blog-item {
				padding: 14px;
			}

			.blog-item:hover,
			.blog-item:focus {
				padding-left: 18px;
			}
		}
	</style>
</head>
<body>
	<nav class="nav">
		<div class="nav-left">
			<a href="/" class="logo">icelain</a>
			<span class="separator">/</span>
			<span class="page-title">blog</span>
		</div>
		<a href="/" class="nav-link">← back to home</a>
	</nav>
	
	<div class="container">
		<div class="header-section">
			<h1>Blog</h1>
			<p class="description">Technical writeups and experiments</p>
		</div>
		
		<div class="blog-list">
			{{range .P}}
			<a href="/blog/{{.DashedTitle}}" class="blog-item">
				<h3 class="title">
					<span class="title-text">{{.Title}}</span>
				</h3>
				<div class="meta">
					<span class="date">{{.Date}}</span>
				</div>
			</a>
			{{end}}
		</div>
	</div>

	<footer>
		<p>icelain@ice.computer — 2025</p>
	</footer>
</body>
</html>
